from groq import Groq

client = Groq(api_key="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")

def analyze_circular(circular_content, company_policy):
    response = client.chat.completions.create(
        model="llama-3.3-70b-versatile",
        messages=[
            {"role": "system", "content": "You are PolicyGuard, an expert AI compliance analyst for Indian companies."},
            {"role": "user", "content": f"Analyze this circular:\n{circular_content}\n\nCompany Policy:\n{company_policy}\n\nGenerate report with: 1.SUMMARY 2.IMPACT LEVEL 3.AFFECTED AREAS 4.REQUIRED ACTIONS 5.DEADLINE 6.PENALTY 7.AMENDMENTS"}
        ],
        temperature=0.3,
        max_tokens=2000
    )
    return response.choices[0].message.content

def generate_amendment(circular_content, policy_section):
    response = client.chat.completions.create(
        model="llama-3.3-70b-versatile",
        messages=[
            {"role": "system", "content": "You are a legal compliance expert for Indian companies."},
            {"role": "user", "content": f"Circular:\n{circular_content}\n\nRewrite this policy to be compliant:\n{policy_section}"}
        ],
        temperature=0.2,
        max_tokens=1000
    )
    return response.choices[0].message.content
def detect_changes(old_circular, new_circular):
    response = client.chat.completions.create(
        model="llama-3.3-70b-versatile",
        messages=[
            {"role": "system", "content": "You are a regulatory change detection expert."},
            {"role": "user", "content": f"""
Compare these two versions of a regulatory circular and detect EXACTLY what changed:

OLD VERSION:
{old_circular}

NEW VERSION:
{new_circular}

List every change as:
- ADDED: [what was added]
- REMOVED: [what was removed]  
- MODIFIED: [what was changed from X to Y]
"""}
        ],
        max_tokens=1000
    )
    return response.choices[0].message.content
def detect_changes(old_circular, new_circular):
    response = client.chat.completions.create(
        model="llama-3.3-70b-versatile",
        messages=[
            {"role": "system", "content": "You are a regulatory change detection expert for Indian companies."},
            {"role": "user", "content": f"""
Compare these two versions of a regulatory circular and detect EXACTLY what changed:

OLD VERSION:
{old_circular}

NEW VERSION:
{new_circular}

List every change clearly as:
✅ ADDED: [what was newly added]
❌ REMOVED: [what was removed]
🔄 MODIFIED: [what changed from X to Y]
⚠️ IMPACT: [why this change matters]

Be specific with numbers, dates and requirements.
"""}
        ],
        temperature=0.3,
        max_tokens=1500
    )
    return response.choices[0].message.content
